public enum RewardType { Heart, Coin, UseItem, BoosterItem, RemovedAD }
public enum Stat { Health, Hunger, Cleaniness, Happiness }

public enum Growth { Egg, Baby, Teen, Adult }

public enum PetFlag { IsSick, IsSleeping, IsLeft }

public enum RarityType { Common, Rare, Epic, Legendary } // 레어 3%, 에픽 1%, 레전더리 0.5%

public enum PartType
{
    Body,
    Arm,
    Feet,
    Eye,
    Ear,
    Mouth,
    Acc,
    Wing,
    Pattern,
    Color,
    Blush,
    Personality
}

public enum LeftReason
{
    Hunger,
    Unhappy,
    Dirty,
    LayEgg,
}

public enum Language
{
    KOR = 0,
    DE = 1,
    ENG = 2,
    JP = 3,
    CH = 4,
}